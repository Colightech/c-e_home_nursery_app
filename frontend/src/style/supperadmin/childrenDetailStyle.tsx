import { StyleSheet } from "react-native";



const styles = StyleSheet.create({
    container : {
        backgroundColor: "#fff",
        elevation: 20,
        padding: 20,
        borderRadius: 20,
    }
})

export default styles;